package com.java8.lambda.chapter5;

/**
 * 	使用收集器
 *	
 *	收集器，一种通用的、从流生成复杂值的结构。
 *
 *	只要将它传给 collect 方法，所有的流就都可以使用它了。
 *
 *	本章示例代码中的收集器都是从 java.util.stream.Collectors 类中静态导入的。
 *
 *
 *	@author hzweiyongqiang
 */
public class Course30EnterCollector {

}
